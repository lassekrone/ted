# Get started

## 1. Install uv if not already installed
`https://docs.astral.sh/uv/getting-started/installation/#installation-methods`

## 2. Initialize uv inside this project
`uv init`

## Adding new packages
`uv add [name of package]`
